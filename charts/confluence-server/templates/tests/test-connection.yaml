apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "confluence-server.fullname" . }}-test-connection"
  labels:
    {{- include "confluence-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "confluence-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
